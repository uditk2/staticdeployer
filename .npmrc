; Use a project-local cache to avoid global ~/.npm EACCES issues
cache=.npm-cache

; Save exact versions for deterministic installs
save-exact=true

; Enforce engines from package.json (Node >=18)
engine-strict=true

; Reduce noise
fund=false
audit=false
